#include <stdio.h>

/**
 * main - print the number of command line argument
 * passed to a programm
 * @argc: the command line argument count
 * @argv: the command line argument vector
 * Return: int
 */
int main(int argc)
{
printf("%d\n", argc  - 1);
return (0);
}
