#include <stdio.h>
#include "main.h"
/**
 * add - add two integers and returns the result
 * @num1 : first number
 * @num2 : second number
 * Return: int
 */
int add(int num1, int num2)
{
return (num1 + num2);
}
